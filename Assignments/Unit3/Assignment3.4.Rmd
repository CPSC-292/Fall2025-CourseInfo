---
title: "Assignment 3.4"
author: "Your Name"
date: "10/29/2025"
output:
  html_document:
    df_print: paged
subtitle: "CPSC 292 Section ##"
---

## Assignment 3.4

```{r, include=TRUE}
# Loading required libraries

```

This assignment uses the same files in a `data` as Assignment 3.3.

### Question 1

*Write a for loop that loads in each data set in the `data/` folder and plots the point 1 x and y values from all 6 sets on the same plot as lines. Be sure to use base graphics and color each line separately.*

```{r, include=TRUE}

```

### Question 2

*Write a for loop that loads in each data set in the `data/` folder and plots the point 1 x and y values from all 6 sets on the same plot as lines. Be sure to use base graphics and color each line separately.*


```{r, include=TRUE}

```
